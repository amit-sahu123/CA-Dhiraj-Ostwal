/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['cloud.appwrite.io'], // Add more domains if needed
    },
};

export default nextConfig;
